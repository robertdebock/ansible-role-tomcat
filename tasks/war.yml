---
- name: Deploy war url
  ansible.builtin.get_url:
    url: "{{ war.url }}"
    dest: "{{ tomcat_directory }}//{{ instance.folder | default(tomcat_folder) }}/webapps/"
    validate_certs: no
  when:
    - war.url is defined
