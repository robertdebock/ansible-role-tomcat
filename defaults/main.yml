---
# defaults file for tomcat
tomcat_layout:
  - name: tomcat
    directory: /opt
    version: 8
    user: tomcat
    group: tomcat
    xms: 512M
    xmx: 1024M
    non_ssl_connector_port: 8080
    ssl_connector_port: 8443
    shutdown_port: 8005
    ajp_port: 8009
    wars:
      - sample.war
